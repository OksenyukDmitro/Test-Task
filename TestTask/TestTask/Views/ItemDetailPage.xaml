<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TestTask.Views.ItemDetailPage"
             Title="{Binding Title}">
    
    <StackLayout Spacing="20" Padding="15">
        <Label Text="{Binding Item.Text ,StringFormat='Text: {0}'}" FontSize="Medium"/>
        <Label Text="{Binding Item.Translate ,StringFormat='Translate: {0}'}" FontSize="Medium"/>
        <Label Text="{Binding Item.Transcript ,StringFormat='Transcript: {0}'}" FontSize="Medium"/>
        <Label Text="Tags: " FontSize="Medium"/>
        <ListView SeparatorVisibility="None" ItemsSource="{Binding Item.Tag}"   
                                          HasUnevenRows="true"
                                         >
            <ListView.ItemTemplate >
                <DataTemplate>
                    <ViewCell>

                        <StackLayout   >
                            <Label Text="{Binding .}" 
                           
                             LineBreakMode="WordWrap " 
                             Style="{DynamicResource ListItemTextStyle}" 
                              FontSize="16" />
                        </StackLayout>

                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>